<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>editing user :: {{user.getName}}</title>
</head>
<body>
    <div>

        <h3>Editing user - '{{user.getName}}'</h3>

        <div>
            <form method="POST" action="/users/my-page/edit">
                <div>
                    <label for="name">Name</label>
                    <input id="name" name="name" value="{{user.getName}}"/>
                    <div>{{#errors}}{{#name}}<div style="color:red">{{.}}</div>{{/name}}{{/errors}}</div>
                </div>
                <div>
                    <label for="email">Email</label>
                    <input id="email" name="email" value="{{user.getEmail}}"/>
                    <div>{{#errors}}{{#email}}<div style="color:red">{{.}}</div>{{/email}}{{/errors}}</div>
                </div>
                <br/>
                <div>
                    <button type="submit">Update user</button>
                </div>
            </form>
        </div>

    </div>
</body>
</html>