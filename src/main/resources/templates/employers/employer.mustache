<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>employer :: {{employer.getName}}</title>
</head>
<body>
    <div>

        <div>
            <h4>Employer</h4>
            <p>Name: <span>{{employer.getName}}</span></p>
            <hr/>
        </div>

        <div>
            {{#isCurrentEmployer}}
                <a href="/employers/{{{employer.getId}}}/edit">Edit</a>
                <hr/>
            {{/isCurrentEmployer}}
        </div>

        <div>
            <p>Vacancies:</p>
            <div>
                {{#isCurrentEmployer}}
                    <b><a href="/vacancies/new">Add new vacancy</a></b>
                    <br/>
                    <br/>
                {{/isCurrentEmployer}}
            </div>
            {{#employer.getVacancies}}
                <div>
                    <a href="/vacancies/{{{getId}}}">{{getTitle}}</a>
                    <br/>
                </div>
            {{/employer.getVacancies}}
        </div>

    </div>
</body>
</html>